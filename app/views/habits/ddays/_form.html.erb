<%= form_with(model: dday, local: true) do |form| %>
    <% if dday.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(dday.errors.count, "error") %> prohibited this dday from being saved:</h2>

            <ul>
                <% dday.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="field">
        <%= form.label :user_id %>
        <%= form.text_field :user_id, id: :dday_user_id %>
    </div>

    <div class="field">
        <%= form.label :project_id %>
        <%= form.text_field :project_id, id: :dday_project_id %>
    </div>

    <div class="field">
        <%= form.label :day %>
        <%= form.date_select :day, id: :dday_day %>
    </div>

    <div class="field">
        <%= form.label :success %>
        <%= form.check_box :success, id: :dday_success %>
    </div>

    <div class="actions">
        <%= form.submit %>
    </div>
<% end %>
